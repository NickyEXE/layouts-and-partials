<%= form_for @superhero do |f| %>
  <%= render partial:"formpart", locals:{f: f} %>
  <%= f.label :team_id %>
  <%= f.collection_select :team_id, Team.all, :id, :name %><br><br>
  <%= f.submit %>
<% end %>
